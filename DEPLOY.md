# ヒトログをアプリとして公開する

ヒトログはPWAとしてインストールできる構成です。公開にはHTTPSが必要です。

## 公開前に重要なこと

現在の `file://` 版と、公開後のHTTPS版は別の保存領域になります。先に設定画面の「書き出す」でバックアップを作り、公開版を開いた後に「読み込む」で復元してください。

## GitHub Pagesで無料公開

1. GitHubで非公開または公開リポジトリを作成します。
2. このフォルダーの全ファイルをリポジトリ直下へ配置します。
3. Settings → Pages → Build and deployment で `Deploy from a branch` を選びます。
4. `main` と `/ (root)` を選んで保存します。
5. 発行されたHTTPS URLをスマホで開きます。
6. ヒトログの設定画面から「インストール」を選びます。

## iPhone

Safariで公開URLを開き、共有 →「ホーム画面に追加」を選びます。iOSではアプリ内のインストールボタンが表示されない場合があります。

## Android

Chromeで公開URLを開くと、設定画面の「インストール」が有効になります。ブラウザメニューの「アプリをインストール」も利用できます。

## 公開物

- `manifest.webmanifest`: アプリ名、色、アイコン
- `sw.js`: オフラインキャッシュ
- `icon-192.png` / `icon-512.png`: スマホ用アイコン
- `index.html`, `style.css`, `app.js`: アプリ本体

人物データは公開先へアップロードされず、各端末のIndexedDBに保存されます。
